元组：另一种有序列表叫元组：tuple。tuple和list非常类似
注意：tuple一旦初始化就不能修改，比如同样是列出同学的名字：

元组特点：
    元组被称为只读列表，不能被修改，所以列表的切片操作也是可以用到元组上的。

元组结构：
    (元素1,元素2,....)

注意：虽然元组的元素不可变，但是它可以包含可变的对象，如list

创建空元组：()
创建只有一个元素的元组：(2,):注意逗号不能省略。

用于：dict的key,函数参数