一：迭代器：生成器都是迭代器，迭代器不一定是生成器：
l = [1,2,3,5]

二:迭代器：list,tuple,dict,string:Iterable

三：什么是迭代器？
    满足两个条件：
    ①：有iter方法（通过这个方法可以把可迭代对象变为迭代器）：
    ②：有next方法

四：使用for来进行遍历可迭代对象时，for为我们做了三件事
    ①：将可迭代对象转换为迭代器，通过调用可迭代对象的__iter__()方法。否则直接用for遍历他们会报错，例如for x in [1,2,3]:就会报错
    ②：调用迭代器的next()方法
    ③：检测是否到迭代器的末尾。
